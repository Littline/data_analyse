# 管理后台密码。设置后，/admin/ 与写文章的接口都需要先登录。
# 不设置则后台完全开放（仅建议在本机使用）。
ADMIN_PASSWORD=换成你自己的密码

# 服务端口（npm run build && npm start 时生效），默认 4321
PORT=4321

# 可选：把文章目录放到别处（默认 ./src/content/blog）
# BLOG_CONTENT_DIR=F:/data/my-blog-posts

